_schema-version: "3.2"
ID: C23C249
version: 0.0.1
modules:
- name: c23c249-approuter
  type: approuter.nodejs
  path: c23c249-approuter
  requires:
  - name: C23C249_html_repo_runtime
  - name: uaa_C23C249
  - name: C23C249-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: C23C249_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: C23C249_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - chchesinvoices.zip
      name: chchesinvoices
      target-path: resources/
- name: chchesinvoices
  type: html5
  path: invoices
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: C23C249_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_C23C249
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: C23C249-xsuaa-service
    service-plan: application
- name: C23C249_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: C23C249-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: C23C249-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
